<%= form_with model:item do |f| %>
  <%= f.label :name, "名前" %>
  <%= f.text_field :name %>
  <%= f.label :value, "金額（半角数字）" %>
  <%= f.text_field :value %>
  <%= select_tag 'item[category]', options_for_select(options, selected: item.category) %>
  <%= f.date_field :date %>
  <%= f.text_area :memo %>
  <%= f.hidden_field :room_id, value: params[:room_id]  %>
  <%= f.submit %>

<% end %>
